apply plugin: "groovy"
apply plugin: 'java-gradle-plugin'

repositories {
    jcenter()
}

dependencies {
    testCompile('org.ajoberstar:grgit:1.7.0')
    testCompile('org.spockframework:spock-core:1.0-groovy-2.4') {
        exclude module: 'groovy-all'
    }
}